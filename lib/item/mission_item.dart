import 'package:flutter/material.dart';

class MissionItem extends StatelessWidget {
  const MissionItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      alignment: Alignment.center,
      child: const Text('티켓 아이템'),
    );
  }
}
